# VoxClone model service dependencies — Kokoro (presets + mixing) + Pocket TTS
# (cloning). CPU-only. torch is installed as a CPU-only build in the Dockerfile
# to keep the image small; the version here just pins the floor.
pocket-tts>=2.1.0
kokoro>=0.9.4
torch>=2.5.0
soundfile
numpy
fastapi
uvicorn[standard]
python-multipart
